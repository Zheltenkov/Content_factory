Перепиши уже переведённый документ на {{ target_language }} для лучшей читаемости.

Нельзя менять структуру Markdown, количество заголовков, таблицы, ссылки и маркеры `[[[BLOCK_N]]]`.
Меняй только формулировки абзацев и пунктов списков. Смысл и полнота должны сохраниться.
Письменность: {{ script_instruction }}.

Документ:
{{ markdown }}
